const ALL_TAB_IDS = [
  'dashboard', 'leads', 'followup-leads', 'proposals',
  'clients', 'projects', 'payments', 'users', 'settings',
];

const DEFAULT_TABS = ['dashboard', 'leads', 'followup-leads', 'settings'];

module.exports = { ALL_TAB_IDS, DEFAULT_TABS };
